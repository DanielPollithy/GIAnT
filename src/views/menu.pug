nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/', class='no-loader') GIAnT
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      ul.nav.navbar-nav
        if selected === 'images'
          li.active
            a(href='/', class='no-loader')
              | Index
              span.sr-only (current)
        else
          li
            a(href='/', class='no-loader') Index
        li
          a(onclick="location.reload();", style="cursor:pointer;", class='no-loader')
            span(class='glyphicon glyphicon-refresh')

      ul.nav.navbar-nav.navbar-right
        li
          a(href='/batch-add-to-neo4j')
            | Batch add&nbsp;
            span(class='glyphicon glyphicon-open')
        if selected === 'heatmap'
          li.active
            a(href='/heatmap', class='no-loader')
              | Heatmap
              span.sr-only (current)
        else
          li
            a(href='/heatmap', class='no-loader') Heatmap
        if selected === 'export'
          li.active
            a(href='/export', class='no-loader')
              | Export
              span.sr-only (current)
        else
          li
            a(href='/export', class='no-loader') Export
        if selected === 'database'
          li.active
            a(href='/db', class='no-loader')
              | Database
              span.sr-only (current)
        else
          li
            a(href='/db', class='no-loader') Database
        if selected === 'settings'
          li.active
            a(href='/settings', class='no-loader')
              | Settings
              span.sr-only (current)
        else
          li
            a(href='/settings', class='no-loader') Settings
        if selected === 'constraints'
          li.active
            a(href='/constraints', class='no-loader')
              | Constraints
              span.sr-only (current)
        else
          li
            a(href='/constraints', class='no-loader') Constraints
