nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='#') Transliteration Application
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      ul.nav.navbar-nav
        if selected === 'images'
          li.active
            a(href='/')
              | Index
              span.sr-only (current)
        else
          li
            a(href='/') Index
        li
          a(onclick="location.reload();", style="cursor:pointer;") Refresh


      ul.nav.navbar-nav.navbar-right
        li
          a(href='/batch-add-to-neo4j') Batch Add
        if selected === 'heatmap'
          li.active
            a(href='/heatmap')
              | Heatmap
              span.sr-only (current)
        else
          li
            a(href='/heatmap') Heatmap
        if selected === 'database'
          li.active
            a(href='/db')
              | Database
              span.sr-only (current)
        else
          li
            a(href='/db') Database
        if selected === 'settings'
          li.active
            a(href='/settings')
              | Settings
              span.sr-only (current)
        else
          li
            a(href='/settings') Settings
